# คู่มือโปรเจกต์ที่รวมแล้ว

## สรุปการเปลี่ยนแปลง

โปรเจกต์ได้รวม frontend และ backend เข้าด้วยกันแล้ว โดยใช้ **backend** เป็นฐาน

### การเปลี่ยนแปลงหลัก:

✅ **Port**: เปลี่ยนจาก 3001 → 3000  
✅ **Components**: คัดลอกทั้งหมดจาก frontend/components → app/src/components  
✅ **Hooks**: คัดลอกจาก frontend/hooks → app/src/hooks  
✅ **Libraries**: รวม lib files เข้าด้วยกัน  
✅ **Dependencies**: รวม packages ทั้งหมดจาก frontend  
✅ **Configurations**: อัพเดท next.config.mjs, tailwind.config.js, tsconfig.json  
✅ **Styles**: รวม CSS และ Tailwind configurations  
✅ **Public Assets**: คัดลอก static files  

## วิธีใช้งาน

### 1. ติดตั้ง Dependencies

```bash
cd backend
npm install
```

### 2. ตั้งค่า Environment Variables

ไฟล์ `.env` ได้ถูกอัพเดทแล้ว โดยเปลี่ยน:
- `NEXTAUTH_URL` จาก `http://localhost:3001` → `http://localhost:3000`
- เพิ่ม `NEXT_PUBLIC_API_URL=http://localhost:3000`
- เพิ่ม `NEXT_PUBLIC_ELEARNING_BASE_URL=http://localhost:3000`

### 3. รันโปรเจกต์

```bash
cd backend
npm run dev
```

เว็บไซต์จะรันที่ **http://localhost:3000**

## โครงสร้างโฟลเดอร์ใหม่

```
app/
├── src/
│   ├── app/
│   │   ├── (site)/              # Frontend pages (จาก frontend)
│   │   │   ├── about/
│   │   │   ├── articles/
│   │   │   ├── books/
│   │   │   ├── cart/
│   │   │   ├── checkout/
│   │   │   ├── courses/
│   │   │   ├── exam-bank/
│   │   │   ├── profile/
│   │   │   └── ...
│   │   ├── admin/               # Admin dashboard
│   │   └── api/                 # API routes
│   ├── components/              # UI Components (รวม frontend)
│   │   ├── ui/                  # Shadcn/ui components
│   │   ├── sections/            # Page sections
│   │   ├── profile/
│   │   ├── pdf/
│   │   └── ...
│   ├── hooks/                   # React hooks
│   ├── lib/                     # Utilities & helpers
│   └── styles/                  # Additional styles
├── prisma/                      # Database schema
├── public/                      # Static files
├── components.json              # Shadcn/ui config
└── package.json                 # รวม dependencies แล้ว
```

## สิ่งที่ไม่ต้องทำอีกต่อไป

❌ ไม่ต้องรัน frontend แยกต่างหาก  
❌ ไม่ต้องใช้ API proxy/rewrites  
❌ ไม่ต้องกังวลเรื่อง CORS  

## การทำงาน

- **Frontend Pages**: เข้าถึงได้ที่ root path (/, /courses, /books, etc.)
- **Admin Pages**: เข้าถึงได้ที่ /admin/*
- **API Routes**: เข้าถึงได้ที่ /api/*

## คำสั่งที่มีประโยชน์

```bash
# Development
npm run dev

# Build for production
npm run build

# Start production server
npm start

# Database commands
npm run db:migrate
npm run db:seed
npm run db:studio

# Type checking
npm run type-check

# Linting
npm run lint
```

## หมายเหตุสำคัญ

1. **โฟลเดอร์ frontend เดิม**: ยังคงอยู่ แต่ไม่จำเป็นต้องใช้แล้ว คุณสามารถลบได้ถ้าต้องการ
2. **Database**: ยังใช้ Prisma เหมือนเดิม ไม่มีการเปลี่ยนแปลง
3. **Authentication**: NextAuth ยังทำงานเหมือนเดิม แค่เปลี่ยน URL
4. **File Uploads**: Cloudinary และ R2 Storage ยังทำงานปกติ

## การ Deploy

เมื่อ deploy ให้แน่ใจว่า:
- อัพเดท environment variables ทั้งหมดให้ชี้ไปที่ production URL
- รัน `npm run build` ก่อน deploy
- ตั้งค่า port เป็น 3000 หรือตามที่ hosting กำหนด

---

**สรุป**: ตอนนี้คุณมีโปรเจกต์เดียวที่รวมทั้ง frontend และ backend เข้าด้วยกัน รันง่ายขึ้น deploy ง่ายขึ้น และจัดการได้สะดวกกว่าเดิม! 🎉
